/**
 * Assessments view module.
 * Assessment list is rendered via static HTML.
 * Interactions (view report → Property, view client → Clients) are handled by setupCrossNavigation in router.ts.
 */
export function initAssessments(): void {
  // Assessment interactions are wired by setupCrossNavigation in router.ts
}
